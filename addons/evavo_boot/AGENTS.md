# Generated EVAVO Boot Package — Agent Rule

This directory is canonical only when it is inside `EVAVO-STUDIO/evavo-game-runtime`.

If you are reading this file from an individual game repository, `addons/evavo_boot` is a generated/vendor package. Do not edit, fork, delete or locally patch it. Make the shared change in `EVAVO-STUDIO/evavo-game-runtime/addons/evavo_boot`, validate the runtime, then propagate it with `EVAVO-STUDIO/GodotGameFoundationKit/tools/bootstrap_evavo_boot_portfolio.ps1`.

For quick tests, use F6/scene-specific launch or `--evavo-skip-splash` / `EVAVO_AGENT_QUICK_TEST=1`. For actual startup/release/splash validation, use `--evavo-full-boot` in a non-headless graphical run.

Never bypass this contract by rewriting Git history or deleting the project boot entrypoint.
